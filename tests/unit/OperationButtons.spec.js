import OperationButtons from '@/components/OperationButtons';

describe('OperationButtons', () => {
  test('If Validation Object is not empty "yes Please" button should not visible', () => {
    expect(true).toBe(true);
  });
  test('If Validation Object is empty "yes Please" button should be visible', () => {
    expect(true).toBe(true);
  });
});
